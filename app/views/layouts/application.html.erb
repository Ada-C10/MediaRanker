<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="app-header__header">
      <h1>
        <%= link_to "Media Ranker", root_path%>
        <small>Ranking the Best of Everything</small>
      </h1>

    <nav class="app-header__nav">
    <ul class="nav app-header__site-nav-container">
      <li class="nav-item app-header__nav_item"><%= link_to "(View Top Works/MainPage)", root_path %></li>
      <li class="nav-item app-header__nav_item"><%= link_to "(View All Works)", works_path %></li>
      <li class="nav-item app-header__nav_item"><%= link_to "(Add A New Work)", new_work_path %></li>
      <li class="nav-item app-header__nav_item"><%= link_to "(View All Users)", users_path %></li>
    </ul>

    <section class="flash-messages">
    <% flash.each do |key, message| %>
      <p class="<%= key %>">
        <%= message %>
      </p>
    <% end %>
    </section>

    <ul class="nav app-header__user-nav-container">
      <li class="nav-item app-header__nav_item">

    <section class="login-info">
      <% logged_in_user = User.find_by(id: session[:user_id]) %>
      <% if logged_in_user %>
        <p>
        Currently logged in as user
        <%= logged_in_user.name %>
        <%= link_to "Log out", logout_path, method: :post, class: "btn btn-outline-info" %>
        </p>
      <% else %>
        <%= link_to "Log in", new_session_path, class: "btn btn-outline-info" %>
        <% end %>
    </section>

    </header>

    <main>
      <%= yield %>
    </main>

  </body>
</html>
