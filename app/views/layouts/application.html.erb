<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="icon" href="../images/owl.jpg">
  </head>
<body>
  <header>
      <section class="flash">
        <% flash.each do |name, message| %>
          <% if message.class == Array %>
            <% message.each do |msg| %>
              <div class="alert-warning">
                <strong><%= "#{name}:" %></strong><%= " #{msg}" %></div>
            <% end %>
          <% else %>
            <div class="alert-<%= name%>">
                <%= message %>
            </div>
          <% end %>
        <% end %>

      </section>

      <header class="app-header_header">
      <h1> Media Ranker |
        <small>Ranking the Best of Everything </small>
        </h1>


      <nav class="nav">
      <ul class="nav site-nav-containe">
        <li class="nav_item">
          <%= link_to  "View top media", "/",class: "btn btn-link", role: "button" %>
        </li>
        <li class="nav_item" >
          <%= link_to  "View all media  ", works_path, class: "btn btn-link", role: "button" %>
        </li>
        <li class="nav_item">
          <%= link_to "Add a new work", new_work_path,class: "btn btn-link", role: "button" %>
        </li>
        <li class="nav_item">
          <%= link_to "View all Users", users_path,class: "btn btn-link", role: "button" %>
        </li>

        <li >
          <% if @current_user %>
          <%= link_to "#{@current_user.name} is logged in", user_path(@current_user.id), class: "btn btn-link", role: "button" %>
          <% end %>
        </li>

        <li >
          <% if @current_user %>
          <%= link_to "log out", logout_path, method: :delete, class: "btn btn-primary", role: "button" %>
          <% else %>
          <%= link_to "Login", login_path,class: "btn btn-primary" , role: "button"%>
          <% end %>
        </li>
          </ul>
        </nav>


    </header>
    <%= yield %>
  </body>
</html>
