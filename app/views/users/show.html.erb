<h1 class="headliner"> User Summary: <%= @user.user_name %> </h1>


<p> Joined site <%= @user.created_at.strftime("%b %d, %Y") %></p>

<h3>Votes</h3>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Media Title</th>
      <th scope="col">Created By</th>
      <th scope="col">Published</th>
      <th scope="col">Category</th>
      <th scope="col">Voted On</th>
    </tr>
  </thead>

  <% @user.votes.each do |vote| %>
  <% if vote.work_id %>
  <tbody>
    <tr>
      <td><%= link_to vote.work.title, work_path(vote.work.id) %></td>
      <td><%= vote.work.creator %></td>
      <td><%= vote.work.publication_year %></td>
      <td><%= vote.work.category %></td>
      <td><%= vote.created_at.strftime("%b %d, %Y") %></td>
    </tr>
    <% end %>
    <% end %>
  </thead>
</table>

<section>
  <%= link_to "See all users", users_path, class: "btn btn-info" %>
  <%= link_to "Back to media list", works_path, class: "btn btn-info" %>
</section>
