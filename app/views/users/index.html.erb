<h1>List of Users</h1>
<ul>
  <strong>Username</strong>
  <% @users.each do |user| %>
  <li>
    <%= link_to "#{user.username}", user_path(user.id) %>
  </li>
  <% end %>
</ul>

<%= link_to "Back to Media List", home_path %>
