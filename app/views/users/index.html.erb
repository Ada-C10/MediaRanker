<h1>These are the users!</h1>
<% @users.each do |user| %>
  <h2><%= link_to "#{user.name}", user_path(user.id) %></h2>
  <p> <%= "Joined: #{user.created_at}" %></p>
  <p> <%= "Number of votes: #{user.votes.length}" %> </p>

<% end %>
