<article>
  <h2>List of Users</h2>
  <section>
    <ul>
      <% @users.each do |user| %>
        <li><%= link_to user.username, user_path(user.id) %></li>
      <% end %>
    </ul>
  </section>
</article>
