<h2>All of the Users!</h2>

<ul>
  <% @users.each do |user| %>
  <li>

    <strong>
    <div>
      <%= link_to "#{user.username}", user_path(user.id) %>
    </div>
  </strong>

  </li>
  <% end %>
</ul>
