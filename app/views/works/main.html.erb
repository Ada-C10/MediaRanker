<main>
      <section class="spotlight">
    <h2 class="spotlight__header">
      <span class="spotlight__header--prefix">Media Spotlight</span>
      <a class="spotlight__link-to" href="/works/109">🐶🐱🐶</a> by WHO DELETED HTE JAMBES CAMBERON ALBUM ????
    </h2>
    <p class="spotlight__description">
      17 votes - do NOT delete my things - sincerly, jambes (guy from titanic)
    </p>
  </section>

<hr class="root__hr">

<section class="top-ten__container">
  <section class="top-ten__list-container">
  <h2 class="top-ten__header">Top Movies</h2>
  <ul class="list-group top-ten__list">
    <%['movie', 'book', 'album'].each do |category| %>
    <% @works.each do |work| %>
    <%if work.category == "movie" %>
      <li class="list-group-item">
        <h4>
          <%=link_to work.title, work_path(work.id) %>
          <small class="top-ten__creator">by <%= work.creator%></small>
        </h4>
        <p><%= work.votes.length %></p>
      </li>
  </ul>
</section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">Top Books</h2>

  <ul class="list-group top-ten__list">
      <%elsif work.category == "book"%>
      <li class="list-group-item">
        <h4>
          <%=link_to work.title, work_path(work.id) %>
          <small class="top-ten__creator">by <%= work.creator%></small>
        </h4>
        <p><%= work.votes.length %></p>
      </li>
  </ul>
  </section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">Top Albums</h2>
  <ul class="list-group top-ten__list">
    <%else work.category == "albums" %>
      <li class="list-group-item">
        <h4>
          <%=link_to work.title, work_path(work.id) %>
          <small class="top-ten__creator">by <%= work.creator%></small>
        </h4>
        <p><%= work.votes.length %></p>
      </li>
  </ul>
  <%end%>
  <%end%>
  <%end%>


  </section>

</section>


  </main>
