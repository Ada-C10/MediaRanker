<!DOCTYPE html>
<html>
  <head>
    <title>Media Ranker</title>
  </head>

  <body>

    <nav class="nav">
      <li><%= link_to "View top media", root_path %></li>
      <li><%= link_to "View all media", works_path %></li>
      <li><%= link_to "Add a new work", new_work_path %></li>
      <li><%= link_to "View all users", users_path %></li>
    </nav>

    <section class="spotlight">
      <h2>"Media Spotlight"</h>
      <p>
        <% work_most = @works.each do { |work| work.vote.max } %>
          <%=link_to work.id, work_path(work.id)%>
      </p>
    </section>



  </body>
</html>
