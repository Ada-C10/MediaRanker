<article>
  <h1>List of Works</h1>
</article>

<article>
  <h2>Albums</h2>
  <section>
    <ul>
        <% @works.each do |work| %>
          <% if work.category == "album" %>
            <li><%= work.title %></li>
          <% end %>
        <% end %>
    </ul>
  </section>
</article>

<article>
  <h2>Books</h2>
  <section>
    <ul>
        <% @works.each do |work| %>
          <% if work.category == "book" %>
            <li><%= work.title %></li>
          <% end %>
        <% end %>
    </ul>
  </section>
</article>

<article>
  <h2>Movies</h2>
  <section>
    <ul>
        <% @works.each do |work| %>
          <% if work.category == "movie" %>
            <li><%= work.title %></li>
          <% end %>
        <% end %>
    </ul>
  </section>
</article>
