
<main>
  <h2>List of Works</h2>
  <section class="media-table">

<%[['movie', @movies],['book', @books], ['album', @albums]].each do |category, works| %>
<h4><%="#{category.upcase}"%>S </h4>
<table class="table">
  <thead>
  <tr>
    <th width="80">Votes</th>
    <th>Title</th>
    <th>Created By</th>
    <th width="100">Published</th>
    <th width="100">Upvote</th>
  </tr>
</thead>
  <% works.each do |work| %>
  <tr>
    <td><%= work.votes.count %></td>
    <td><%=link_to work.title, work_path(work.id) %></td>
    <td><%=work.creator%></td>
    <td><%=work.publication_year%></td>
    <td><%= link_to "Upvote", upvote_path(work.id), method: :post, class: "btn btn-primary" %></td>
  </tr>
  <%end%>
</table>
<%end%>

<a class="btn btn-secondary" href="/">View top media</a>
<%=link_to "Add a new work", new_work_path, class: "btn btn-primary" %>

  </main>
