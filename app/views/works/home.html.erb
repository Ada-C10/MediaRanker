<h1>Home page</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Top Movies</th>
      <th scope="col">Top Books</th>
      <th scope="col">Top Albums By</th>
    </tr>
  </thead>
  <tbody>

    <%= @albums.each do |album| %>
    <tr>
    <th scope="row"><%= "#{album.total_votes}"%></th>
    <p>
    <% end %>
  </tr>
</tbody>
  </table>
