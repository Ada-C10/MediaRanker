<section>
 <h2><%=@work.title%></h2>
 <p>Created by: <%=@work.creator%></p>
 <p>Published: <%=@work.publication_year%></p>
 <p><%=@work.description%></p>
 <section>
   TO DO: ADD LINK TO UPVOTE BUTTON
   <a class="btn btn-primary" href="/">Back to media ranks</a>
   <%=link_to "Edit", edit_work_path(@work),{ :class=>"btn btn-primary" }%>
   <%=link_to "Upvote", root_path, { :class=>"btn btn-primary" }%>
   <%=link_to "Delete", work_path(@work),{ :class=>"btn btn-danger", method: :delete, data: { confirm: "Are you sure you want to delete this #{@work.category} '#{@work.title}'?"} }%>
 </section>
</section>

<section class="works-votes__container">
  <h3 class="works-votes__header">[NUMBER] Votes for this <%=@work.category%></h3>
  <table class="table">
    <thead>
      <tr>
        <th>User</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <tr> TO DO: POPULATE WITH ERB
        <td><a href="/users/[NUM]"> LOOP THROUGH VOTES.USER_ID</a></td>
        <td>LOOP THROUGH VOTES.CREATION_DATE</td>
</section>
