<h1>Please Log in</h1>

<%= form_with model: @user, url: login_path, method: method do |f| %>
  <%= f.label :username, 'Username' %>
  <%= f.text_field :username, placeholder: 'Username' %>
  <%= f.submit action_button %>
<% end %>
