<section class='spotlight'>
  <h2>Media Spotlight</h2>
</section>
<section class='top-lists'>
  <div>
    <h2 class='list-titles'>
      Top Movies
    </h2>
    <ul>
      <% @works.each do |work| %>
        <% if work.category == 'movie' %>
        <li>
          <strong><%= work.title %></strong> by
          <%= work.creator %>
        </li>
        <% end %>
      <% end %>
    </ul>
  </div>
  <div>
    <h2 class='list-titles'>
      Top Books
    </h2>
    <ul>
      <% @works.each do |work| %>
        <% if work.category == 'book' %>
        <li>
          <strong><%= work.title %></strong> by
          <%= work.creator %>
        </li>
        <% end %>
      <% end %>
    </ul>
  </div>
  <div>
    <h2 class='list-titles'>
      Top Albums
    </h2>
    <ul>
      <% @works.each do |work| %>
        <% if work.category == 'album' %>
        <li>
          <strong><%= work.title %></strong> by
          <%= work.creator %>
        </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</section>
