votes index page
<br>
aka will be list of top works

Books
<table>
  <thead>
    <tr>
      <th width=80>Votes(loljk work id)</th>
      <th>Title</th>
      <th>Created By</th>
      <th width=100>Published</th>
      <th width=100>Upvote</th>
    </tr>
  </thead>
  <tbody>
    <% @works.each do |work| %>
    <% if work.category == "book" %>
      <tr>
        <td><%= work.votes.count %></td>
        <td><%= link_to work.title, work_path(work) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= link_to "Upvote", votes_path(vote:{work_id: work.id}), class: "button upvote", method: :post %></td>
      </tr>
    <% end %>
    <% end %>

  </tbody>
</table>

Albums
<table>
  <thead>
    <tr>
      <th width=80>Votes(loljk work id)</th>
      <th>Title</th>
      <th>Created By</th>
      <th width=100>Published</th>
      <th width=100>Upvote</th>
    </tr>
  </thead>
  <tbody>
    <% @works.each do |work| %>
    <% if work.category == "album" %>
      <tr>
        <td><%= work.id %></td>
        <td><%= link_to work.title, work_path(work) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= link_to "Upvote", vote_path(work), class: "button upvote", method: :post %></td>
      </tr>
    <% end %>
    <% end %>

  </tbody>
</table>
