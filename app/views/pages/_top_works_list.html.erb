<h2><%= list_name %></h2>
<ul>

  <% model.sort_by_votes.first(10).each do |work| %>
  <li>
    <%= work.title %> by <%= work.creator %>
  </li>
  <li>
    <%= work.votes.length %> votes
  </li>
  <% end %>
</ul>
